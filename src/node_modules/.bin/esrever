#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../esrever/bin/esrever" "$@"
  ret=$?
else 
  node  "$basedir/../esrever/bin/esrever" "$@"
  ret=$?
fi
exit $ret
